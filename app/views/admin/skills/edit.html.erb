<h1>Editing skill</h1>

<%= form_for @skill do |f| %>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= link_to 'Show', @skill %> |
<%= link_to 'Back', skills_path %>
